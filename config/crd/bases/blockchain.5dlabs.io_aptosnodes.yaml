---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: aptosnodes.blockchain.5dlabs.io
spec:
  group: blockchain.5dlabs.io
  names:
    kind: AptosNode
    listKind: AptosNodeList
    plural: aptosnodes
    singular: aptosnode
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AptosNode is the Schema for the aptosnodes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AptosNodeSpec defines the desired state of AptosNode
            properties:
              replicas:
                description: Number of replicas
                type: integer
                default: 1
              nodeType:
                description: Type of node (validator, fullnode)
                type: string
                enum:
                - validator
                - fullnode
                default: validator
              apiPort:
                description: API port
                type: integer
                default: 8080
              grpcPort:
                description: GRPC port
                type: integer
                default: 50051
              resources:
                description: Compute resources
                properties:
                  cpuRequest:
                    description: CPU request
                    type: string
                    default: "32"
                  memoryRequest:
                    description: Memory request
                    type: string
                    default: "64Gi"
                  cpuLimit:
                    description: CPU limit (optional)
                    type: string
                  memoryLimit:
                    description: Memory limit (optional)
                    type: string
                type: object
              config:
                description: Node configuration
                properties:
                  chainId:
                    description: Chain ID
                    type: string
                    default: "mainnet"
                  enableIndexer:
                    description: Enable indexer
                    type: boolean
                    default: false
                  enableTelemetry:
                    description: Enable telemetry
                    type: boolean
                    default: true
                  pruneTxnState:
                    description: Prune transaction state
                    type: boolean
                    default: false
                  dbPath:
                    description: Database path
                    type: string
                    default: "/opt/aptos/data"
                type: object
              image:
                description: Container image
                type: string
                default: "aptos-node:latest"
              identitySecret:
                description: Identity secret name
                type: string
              waypoint:
                description: Waypoint for syncing
                type: string
              genesisVersion:
                description: Genesis version
                type: integer
                default: 0
            required:
            - identitySecret
            type: object
          status:
            description: AptosNodeStatus defines the observed state of AptosNode
            properties:
              phase:
                description: Current phase
                type: string
                enum:
                - pending
                - initializing
                - running
                - error
              version:
                description: Current version
                type: integer
              synced:
                description: Whether the node is synced
                type: boolean
              peers:
                description: Number of connected peers
                type: integer
            type: object
        type: object
    served: true
    storage: true